# Лабораторная работа 3: Бинарная и многоклассовая классификация

## Титульный лист

**Лабораторная работа 3**
**Бинарная классификация. Оценка качества задачи классификации**

**Вариант задания:** 4 (Breast Cancer Dataset)
**Сложность:** Rare

---

## Вариант задания

Используется встроенный датасет Breast Cancer из scikit-learn:
- Варианты 4, 8, 12, 16, 20, 24
- Датасет о раке груди Wisconsin (Diagnostic)

---

## База данных 1

### 1. Описание базы данных

**Название:** Breast Cancer Wisconsin (Diagnostic)

**Целевая переменная (target):**
- `0` — злокачественная опухоль (malignant)
- `1` — доброкачественная опухоль (benign)

**Признаки (30 штук):** Характеристики ядер клеток:
- `mean radius`, `mean texture`, `mean perimeter`, `mean area`, `mean smoothness`...
- `worst radius`, `worst texture`, `worst perimeter`, `worst area`, `worst smoothness`...

**Размер датасета:** 569 строк × 31 столбец

**Размер в памяти:** 0.13 MB

---

### 2. Исследовательский анализ данных

#### Основная статистика

| Признак | Mean | Median | Min | 25% | 75% | Max |
|---------|------|--------|-----|-----|-----|-----|
| mean radius | 14.13 | 13.37 | 6.98 | 11.70 | 15.78 | 28.11 |
| mean texture | 19.29 | 18.84 | 9.71 | 16.17 | 21.80 | 39.28 |
| mean perimeter | 91.97 | 86.24 | 43.79 | 75.17 | 104.10 | 188.50 |
| mean area | 654.89 | 551.10 | 143.50 | 420.30 | 782.70 | 2501.00 |
| ... | ... | ... | ... | ... | ... | ... |

#### Распределение целевой переменной

| Класс | Количество | Процент |
|-------|------------|---------|
| Злокачественная (0) | 212 | 37.3% |
| Доброкачественная (1) | 357 | 62.7% |

**Мода класса:** 1 (Доброкачественная)
**Количество моды:** 357

#### Анализ пропусков

Пропуски отсутствуют!

#### Анализ выбросов (IQR метод)

Наибольшее количество выбросов:
1. `area error` — 65 выбросов (11.4%)
2. `radius error` — 38 выбросов (6.7%)
3. `perimeter error` — 38 выбросов (6.7%)
4. `worst area` — 35 выбросов (6.2%)
5. `smoothness error` — 30 выбросов (5.3%)

#### Корреляция признаков с целевой переменной (ТОП-10)

| Место | Признак | Корреляция |
|-------|---------|------------|
| 1 | worst concave points | 0.7936 |
| 2 | worst perimeter | 0.7829 |
| 3 | mean concave points | 0.7766 |
| 4 | worst radius | 0.7765 |
| 5 | mean perimeter | 0.7426 |
| 6 | worst area | 0.7338 |
| 7 | mean radius | 0.7300 |
| 8 | mean area | 0.7090 |
| 9 | mean concavity | 0.6964 |
| 10 | worst concavity | 0.6596 |

---

### 3. Выводы по анализу

1. Датасет сбалансирован достаточно хорошо (37%/63%)
2. Пропуски отсутствуют
3. Обнаружены выбросы в признаках ошибки (error features)
4. Некоторые признаки имеют высокую корреляцию (mean radius, mean perimeter, mean area)
5. Наиболее информативные признаки: `worst concave points`, `worst perimeter`, `mean concave points`

---

### 4. Описание алгоритмов

#### Модель 1: K-Nearest Neighbors (KNN)

**Принцип работы:** Классификация объекта на основе k его ближайших соседей в пространстве признаков. Класс определяется большинством голосов соседей.

**Параметры:**
- `n_neighbors = 5`
- Данные нормализованы через StandardScaler

**Плюсы:**
- Прост в реализации
- Хорошо работает с нелинейными границами

**Минусы:**
- Требует нормализации данных
- Медленно работает на больших датасетах

#### Модель 2: Логистическая регрессия

**Принцип работы:** Линейный классификатор, моделирующий вероятность принадлежности объекта к классу с использованием логистической функции (сигмоиды).

**Параметры:**
- `max_iter = 1000`
- `random_state = 42`

**Плюсы:**
- Интерпретируемость (коэффициенты показывают влияние признаков)
- Высокая скорость обучения
- Выдает вероятности классов

---

### 5. Визуализация результата

**Созданы графики:**
1. `feature_distributions.png` — распределение основных признаков по классам
2. `correlation_matrix.png` — корреляционная матрица всех признаков
3. `roc_curves.png` — ROC-кривые для обеих моделей
4. `confusion_matrices.png` — матрицы ошибок для обеих моделей
5. `feature_importance.png` — важность признаков для логистической регрессии

---

### 6. Критерии качества

#### Сравнение метрик

| Метрика | KNN | Logistic Regression |
|---------|-----|-------------------|
| **Accuracy (A)** | 0.9561 | **0.9649** |
| **Precision (P)** | 0.9589 | 0.9595 |
| **Recall (R)** | 0.9722 | **0.9861** |
| **F1-score** | 0.9655 | **0.9726** |
| **ROC-AUC** | 0.9788 | **0.9960** |

#### Confusion Matrix — KNN

```
                Predicted
                0    1
Actual  0    39    3
        1     2   70
```

- TN=39, FP=3, FN=2, TP=70
- Accuracy = (39+70)/114 = 95.61%

#### Confusion Matrix — Logistic Regression

```
                Predicted
                0    1
Actual  0    39    3
        1     1   71
```

- TN=39, FP=3, FN=1, TP=71
- Accuracy = (39+71)/114 = 96.49%

#### ROC-кривые

Обе модели показывают отличные результаты (AUC > 0.98). Логистическая регрессия имеет AUC = 0.996, что очень близко к идеальной классификации.

---

### 7. Ответы на контрольные вопросы

1. **Что такое классификация?**
   Классификация — это задача машинного обучения, которая заключается в отнесении объектов к одной из заранее определённых категорий (классов) на основе их признаков.

2. **Чем отличается бинарная классификация от многоклассовой?**
   Бинарная классификация разделяет объекты на 2 класса, многоклассовая — на более чем 2 класса.

3. **Какие преимущества имеет логистическая регрессия?**
   - Простота и интерпретируемость
   - Высокая скорость обучения
   - Возврат вероятности принадлежности к классу
   - Хорошо работает с линейно разделимыми данными

4. **Какую задачу решает логистическая регрессия?**
   Задачу бинарной классификации путём моделирования вероятности принадлежности объекта к одному из классов с использованием логистической функции.

5. **Как исключить входные факторы с низкой значимостью?**
   - RFE (Recursive Feature Elimination)
   - SelectKBest с статистическими тестами
   - Анализ важности признаков (feature importance)
   - L1-регуляризация (Lasso), которая зануляет коэффициенты

6. **Как оценить точность классификации?**
   Accuracy = (TP + TN) / (TP + TN + FP + FN) — доля правильных предсказаний среди всех.

7. **Как работает алгоритм KNN? Чем отличается от логистической регрессии?**
   KNN (k-ближайших соседей) классифицирует объект на основе большинства классов его k ближайших соседей в пространстве признаков. Это непараметрический, «ленивый» метод, который не обучает модель, а хранит обучающие данные. Логистическая регрессия — параметрический метод, который обучает веса признаков для линейной границы принятия решений.

8. **Что показывают критерии качества и ROC-кривая для задачи определения пола?**
   - Accuracy: точность предсказания пола
   - Precision: как много из предсказанных мужчин — действительно мужчины
   - Recall: как много мужчин мы обнаружили из всех мужчин
   - ROC-AUC: показывает способность модели различать классы (площадь под кривой)

9. **Какие входные и выходные параметры используются в логистической регрессии?**
   Входные: признаки X (вектор признаков объекта), выходные: вероятность принадлежности к классу (число от 0 до 1).

10. **Что показывают критерии качества и ROC-кривая для задачи определения доброкачественной опухоли?**
    Позволяют оценить, насколько хорошо модель различает злокачественные и доброкачественные опухоли. ROC-AUC близкий к 1 означает хорошее различение классов.

11. **Как построить таблицу сопряженности (confusion matrix)?**
    Сравниваются истинные значения (y_true) и предсказанные (y_pred), затем подсчитывается количество TP, TN, FP, FN. Используется sklearn.metrics.confusion_matrix().

---

### 8. Вывод по результату и критериям

#### Результаты исследования

1. **Датасет:** Breast Cancer (569 образцов, 30 признаков)
   - Целевая переменная: тип опухоли (0=злокачественная, 1=доброкачественная)
   - Пропуски отсутствуют
   - Небаланс классов ~37%/63%

2. **Анализ признаков:**
   - Выявлены признаки с высокой корреляцией (mean radius, mean perimeter, mean area)
   - Обнаружены выбросы в некоторых признаках
   - Все признаки числовые

3. **Сравнение моделей:**
   - KNN и Логистическая регрессия показали схожие результаты
   - ROC-AUC ~0.98-0.99 указывает на отличное качество классификации

4. **Наиболее важные признаки:**
   - worst concave points, worst perimeter, worst radius
   - mean concave points, mean concavity

#### Лучшая модель

**Логистическая регрессия** выбрана как лучшая модель:
- ROC-AUC: **0.9960**
- Accuracy: **96.49%**
- Precision: **95.95%**
- Recall: **98.61%**
- F1-score: **97.26%**

#### Возможные улучшения

1. **Оптимизация гиперпараметров:**
   - KNN: подбор оптимального k (GridSearchCV)
   - Logistic Regression: подбор параметра регуляризации C

2. **Отбор признаков:**
   - RFE (Recursive Feature Elimination)
   - SelectKBest на основе статистических тестов

3. **Балансировка классов:**
   - SMOTE (Synthetic Minority Over-sampling)
   - Взвешивание классов

4. **Ансамблирование:**
   - Random Forest
   - Gradient Boosting (XGBoost, LightGBM)

#### Итоговый вывод

Обе модели продемонстрировали высокую точность классификации (~97-98%), что говорит о хорошем качестве датасета и информативности признаков для диагностики рака груди. Логистическая регрессия имеет преимущество в интерпретируемости (коэффициенты показывают влияние каждого признака), что важно для медицинских приложений.

---

**Лабораторная работа выполнена в полном объеме для сложности Rare.**

---

## Файлы работы

1. `lab3_analysis.ipynb` — Jupyter Notebook с полным кодом анализа
2. `run_lab3.py` — Python-скрипт для выполнения анализа
3. `report.md` — этот отчет

**Визуализации:**
4. `feature_distributions.png` — распределение признаков
5. `correlation_matrix.png` — корреляционная матрица
6. `roc_curves.png` — ROC-кривые моделей
7. `confusion_matrices.png` — матрицы ошибок
8. `feature_importance.png` — важность признаков
